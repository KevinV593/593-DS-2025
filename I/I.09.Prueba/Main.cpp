#include <iostream>

using namespace std;

int main() {   
    /*
    trazaSec(int*** matriz, int& p, int& f, int& c) {
       acum = c - 1; 
        for (int i = 0, i <= p, i++) {
            for (int j = c, j <= 0, j--) {
                traza += *(*(matriz + i) + j) + (acum)
            }
        }
    }
    */
}
//Reservar, Insertar, Mover (Apuntar)

//I want a proyect in c++ based in OOP, 
//also make two functions that make a insertion of one element 
//to a simple list that has like a tributes a pointer and data int. 
//The functions need to insert elements by the head and the tail

//DEBUG: Parar (breakPoint), Revisar los valores, Ejecutar sentencia 